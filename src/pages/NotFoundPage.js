import React from "react"

import { Layout, SeoPage } from "../components/common"

const NotFoundPage = () => (
  <Layout>
    <SeoPage title="404: Not found" />
    <h1>NOT FOUND</h1>
    <p>OOPS! You just hit a page that doesn&#39;t exist...</p>
  </Layout>
)

export default NotFoundPage
